const input = require('../input');
const log = console.log;

const universe = input(__dirname, './input.example.txt').split('\n');
const expand = (universe) => {};

log(`Solution pt.1 ${1}`);
log(`Solution pt.2 ${2}`);
